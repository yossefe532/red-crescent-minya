# 📋 خطة عمل Telegram Bot Integration — نظام تسجيل المهمات

## 🎯 الهدف النهائي

إنشاء بوت تليجرام مخصص يربط كل أوامر الأدمن بنظام التسجيل، بحيث الأدمن يقدر يتحكم في المهمات كاملة من غير ما يفتح الواجهة.

---

## 🏗️ البنية العامة

```
┌─────────────────┐      ┌──────────────────────┐      ┌─────────────────┐
│  Telegram Bot   │─────▶│ Cloudflare Worker      │─────▶│ D1 Database     │
│  (Node.js/TS)   │      │ (Hono API + Telegram)  │      │ (SQLite)        │
└─────────────────┘      └──────────────────────┘      └─────────────────┘
        │                        │                              │
        │    /start /help       │  /create /toggle /list        │
        │    /stats /cancel      │  /close /export /notifications│
        └────────────────────────┘                              │
                          │                                      │
                          ▼                                      ▼
                   ┌──────────────┐                      ┌──────────────┐
                   │ R2 Storage   │                      │ Audit Log    │
                   │ (audio files)│                      │ (operations) │
                   └──────────────┘                      └──────────────┘
```

---

## 📦 المكونات المطلوبة

### 1. بوت تليجرام (Telegram Bot)
| العنصر | التفاصيل |
|--------|----------|
| **المنصة** | @BotFather على تليجرام |
| **اللغة** | Node.js + TypeScript |
| **المكتبة** | `telegraf` أو `grammy` (أختار الأخف) |
| **الاستضافة** | Cloudflare Worker (يعمل مع D1 مباشرة) |
| **التكامل** | يتواصل مباشرة مع نفس D1 Database |

### 2. Worker جديد (Telegram Handler)
- ملف جديد: `backend/src/routes/telegram.ts`
- يستقبل webhooks من Telegram
- ينفذ أوامر الأدمن
- يرسل إشعارات للمدير

### 3. تحديث Env
- إضافة `TELEGRAM_BOT_TOKEN` في wrangler.toml

---

## 📋 الأوامر المطلوبة في البوت

### 🔐 إدارة المهمات
| الأمر | الوظيفة | مثال |
|--------|----------|------|
| `/create` | إنشاء مهمة جديدة | `/create عنوان:مهمة المنيا السعة:50 الموقع:مركز المنيا` |
| `/list` | عرض كل المهمات | `/list` أو `/list active` |
| `/stats` | إحصائيات مهمة | `/stats ABC123` |
| `/toggle` | فتح/قفل التسجيل | `/toggle ABC123 open` أو `/toggle ABC123 close` |
| `/details` | تعديل تفاصيل | `/details ABC123 title:اسم جديد` |
| `/close` | إغلاق نهائي | `/close ABC123` |
| `/delete` | حذف مهمة | `/delete ABC123` |

### 👥 إدارة التسجيلات
| الأمر | الوظيفة | مثال |
|--------|----------|------|
| `/registrants` | قائمة المشاركين | `/registrants ABC123` |
| `/cancel` | إلغاء تسجيل | `/cancel ABC123 REG-ID` |
| `/export` | تصدير CSV | `/export ABC123` |
| `/waitlist` | عرض قائمة الانتظار | `/waitlist ABC123` |

### 🔔 الإشعارات
| الأمر | الوظيفة | مثال |
|--------|----------|------|
| `/notify` | تفعيل/تعطيل الإشعارات | `/notify on` |
| `/notifications` | حالة الإشعارات | `/notifications` |

### ℹ️ مساعدة
| الأمر | الوظيفة |
|--------|----------|
| `/start` | رسالة ترحيب + قائمة الأوامر |
| `/help` | مساعدة مفصلة |
| `/status` | حالة البوت والخادم |

---

## 🔔 نظام الإشعارات التلقائية

### متى يتبعت إشعار؟
1. **اكتمال السعة** — أول ما العدد يكتمل يتبعت رسالة للأدمن
2. **إغلاق المهمة** — عند إغلاق المهمة نهائياً
3. **تسجيل جديد** — عند تسجيل متطوع جديد
4. **إلغاء تسجيل** — عند إلغاء تسجيل مع إعادة ترتيب القائمة
5. **فتح/قفل التسجيل** — عند تغيير حالة التسجيل

### محتوى الإشعار:
```
🚨 مهمة ABC123 اكتملت السعة!
👥 المؤكدين: 50/50
📋 قائمة الانتظار: 3 متطوعين
```

---

## 🛠️ خطوات التنفيذ (مرتبة)

### المرحلة 1: البنية التحتية (يوم ١)
1. إنشاء البوت عبر @BotFather — أخذ Token
2. إضافة `TELEGRAM_BOT_TOKEN` في wrangler.toml
3. إنشاء ملف `backend/src/routes/telegram.ts`
4. ربط webhook مع Telegram API

### المرحلة 2: الأوامر الأساسية (يوم ٢)
5. `/start` و `/help`
6. `/list` — عرض كل المهمات
7. `/create` — إنشاء مهمة جديدة
8. `/stats` — إحصائيات مهمة

### المرحلة 3: إدارة كاملة (يوم ٣)
9. `/toggle` — فتح/قفل التسجيل
10. `/close` — إغلاق نهائي
11. `/details` — تعديل تفاصيل
12. `/delete` — حذف مهمة

### المرحلة 4: إدارة التسجيلات (يوم ٤)
13. `/registrants` — قائمة المشاركين
14. `/cancel` — إلغاء تسجيل
15. `/waitlist` — قائمة الانتظار
16. `/export` — تصدير CSV

### المرحلة 5: الإشعارات (يوم ٥)
17. نظام إشعارات TURN_ON/OFF
18. إشعار اكتمال السعة (تلقائي)
19. إشعار تسجيل جديد (تلقائي)
20. إشعار إغلاق مهمة (تلقائي)

### المرحلة 6: الاختبار والإطلاق (يوم ٦)
21. اختبار كل الأوامر
22. اختبار الإشعارات التلقائية
23. نشر على Cloudflare
24. ربط webhook نهائي

---

## 📁 الملفات الجديدة المطلوبة

```
backend/
├── src/
│   ├── routes/
│   │   ├── telegram.ts      ← ملف البوت الجديد
│   │   └── ...existing...
│   ├── services/
│   │   └── telegram.service.ts  ← خدمة البوت
│   └── index.ts              ← تحديث لإضافة route
├── wrangler.toml             ← إضافة TELEGRAM_BOT_TOKEN
└── ...existing...
```

---

## ⚙️ الإعدادات المطلوبة منك

### ١. إنشاء البوت:
1. افتح Telegram → @BotFather
2. `/newbot` → اختار اسم مثل "RedCrescent Minya Admin"
3. اختار username مثل "redcrescent_minya_bot"
4. **انسخ الـ Token** (يبقى سري)

### ٢. إعداد الـ Webhook:
```
https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook?url=https://<worker-url>/telegram
```

### ٣. إضافة الـ Token للنظام:
```
wrangler secret put TELEGRAM_BOT_TOKEN
```

---

## 🔗 التوافق مع النظام الحالي

| النظام الحالي | البوت الجديد | الحالة |
|--------------|-------------|--------|
| إنشاء مهمة (web) | `/create` | ✅ نفس الـ D1 |
| فتح/قفل تسجيل | `/toggle` | ✅ نفس الـ API |
| إغلاق مهمة | `/close` | ✅ نفس المنطق |
| تعديل التفاصيل | `/details` | ✅ نفس التحقق |
| حذف مهمة | `/delete` | ✅ نفس العملية |
| عرض التسجيلات | `/registrants` | ✅ نفس الاستعلام |
| تصدير CSV | `/export` | ✅ نفس التنسيق |
| إلغاء تسجيل | `/cancel` | ✅ نفس الـ logic |
| إشعارات التليجرام | تلقائي | 🆕 جديد |

---

## 🎨 تصميم الردود في البوت

### رسالة `/start`:
```
🔴 مرحباً بك في بوت إدارة مهمات الهلال الأحمر — المنيا

📋 الأوامر المتاحة:
/create — إنشاء مهمة جديدة
/list — عرض كل المهمات
/stats — إحصائيات مهمة
/toggle — فتح/قفل التسجيل
/close — إغلاق مهمة نهائيًّا
/delete — حذف مهمة
/registrants — قائمة المشاركين
/cancel — إلغاء تسجيل
/waitlist — قائمة الانتظار
/export — تصدير CSV
/notify — تفعيل الإشعارات
/help — مساعدة مفصّلة

📞 للدعم: تواصل مع فريق التطوير
```

### رسالة اكتمال السعة (إشعار تلقائي):
```
🚨 اكتملت السعة!

📌 المهمة: ABC123 — مهمة المنيا
👥 المؤكدين: 50/50 ✅
📋 قائمة الانتظار: 3 متطوعين
⏰ الوقت: ٢٠٢٦-٠٩-١٠ ١٤:٣٠
```

---

## 💡 ملاحظات مهمة

1. **الأمان**: البوت يستخدم نفس نظام مصادقة الأدمن (جلسات D1)
2. **السرعة**: Cloudflare Worker = استجابة فورية بدون تأخير
3. **التوافق**: يعمل جنب النظام الحالي بدون تعارض
4. **الإشعارات**: تبعث تلقائياً أول ما السعة تكتمل
5. **الصوت**: الإشعارات ممكن تحتوي على رابط تسجيل صوتي (R2)
6. **القائمة**: تعرض بترتيب زمني عكسي (الأحدث أولاً)
7. **التصدير**: CSV بنفس التنسيق الموجود في الويب

---

## ✅ خطوات البدء الفورية

١. أنشئ البوت عبر @BotFather → خلك الـ Token
٢. ابعتلي الـ Token (أو ضعه في wrangler secret)
٣. نبدأ نكتب الكود — أول مرحلة: `/start` + `/list`
٤. ننشر ونختبر

---

## 📊 ملخص الجهد

| المرحلة | الأيام | الصعوبة |
|---------|--------|---------|
| البنية التحتية | ½ يوم | ⭐⭐ |
| الأوامر الأساسية | 1 يوم | ⭐⭐⭐ |
| الإدارة الكاملة | 1 يوم | ⭐⭐⭐ |
| إدارة التسجيلات | 1 يوم | ⭐⭐⭐⭐ |
| الإشعارات | ½ يوم | ⭐⭐⭐ |
| الاختبار والإطلاق | ½ يوم | ⭐⭐ |
| **الإجمالي** | **~٥ أيام** | — |

---

**الجملة اللي نبدأ بيها:**
> "يا يوسف، عايز نبدأ بإنشاء البوت؟ ابعتلي الـ Token من @BotFather ونبدأ نكتب الكود."
